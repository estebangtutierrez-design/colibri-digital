================================================================================
  C# MASTER BOOK — MÓDULO VIII: PROYECTO PROFESIONAL
  CAPÍTULO 71: ARQUITECTURA EN C#
================================================================================

  BIENVENIDA
  ──────────
  Bienvenido al Módulo VIII, el tramo final de C# MASTER BOOK. Has recorrido
  un largo camino desde las variables y los bucles hasta la programación
  orientada a objetos, colecciones, LINQ, async/await y más. Ahora es momento
  de convertirte en un verdadero Arquitecto de Software. Este capítulo sienta
  las bases de todo proyecto profesional: la arquitectura limpia, el diseño
  por capas y la inyección de dependencias.

  FRASE DEL DÍA
  ─────────────
  "La arquitectura no es solo el esqueleto del software; es el mapa que guía
   a todo el equipo hacia un destino común." — Profesor Búho AI

  OBJETIVOS DEL CAPÍTULO
  ─────────────────────
  • Comprender qué es la arquitectura de software y por qué es crucial.
  • Dominar la arquitectura en capas (presentación, negocio, datos).
  • Implementar Inyección de Dependencias (DI) en C#.
  • Introducción a ASP.NET Core y su pipeline.
  • Diferenciar entre acoplamiento y cohesión.
  • Aplicar el principio de inversión de dependencias (DIP).

================================================================================
  [CONTENIDO TEÓRICO]
================================================================================

  1. INTRODUCCIÓN A LA ARQUITECTURA DE SOFTWARE
  ──────────────────────────────────────────────
  La arquitectura de software es la organización fundamental del sistema,
  incluyendo sus componentes, las relaciones entre ellos y el entorno. Una
  buena arquitectura permite:
    • Escalabilidad: crecer sin reescribir.
    • Mantenibilidad: cambios locales sin efectos colaterales.
    • Testeabilidad: probar cada capa de forma aislada.
    • Despliegue independiente: mover componentes a servidores distintos.

  Sin una arquitectura definida, el código se convierte en "código espagueti"
  donde cualquier cambio rompe todo.

  2. ARQUITECTURA EN CAPAS (LAYERED ARCHITECTURE)
  ────────────────────────────────────────────────
  Es el patrón más usado en aplicaciones empresariales. Divide el sistema en
  tres (o cuatro) capas horizontales:

    ┌─────────────────────────────────────────┐
    │       CAPA DE PRESENTACIÓN (UI)         │  → Web API, MVC, Blazor
    ├─────────────────────────────────────────┤
    │       CAPA DE NEGOCIO (BLL)             │  → Lógica, reglas, DTOs
    ├─────────────────────────────────────────┤
    │       CAPA DE DATOS (DAL)               │  → EF Core, repositorios
    ├─────────────────────────────────────────┤
    │       CAPA DE INFRAESTRUCTURA           │  → Logging, mailing, etc.
    └─────────────────────────────────────────┘

  Regla de oro: cada capa solo conoce a la inmediata inferior. La presentación
  conoce al negocio, el negocio conoce a los datos, los datos no conocen a nadie.

  3. INYECCIÓN DE DEPENDENCIAS (DI)
  ─────────────────────────────────
  DI es un patrón de diseño donde los objetos reciben sus dependencias desde
  el exterior en lugar de crearlas internamente. Esto desacopla el código,
  facilitando pruebas y cambios.

  En .NET, el contenedor de DI está integrado en Microsoft.Extensions.DependencyInjection.

  Ciclos de vida:
    • Transient: se crea cada vez que se solicita.
    • Scoped: se crea una vez por solicitud (HTTP request).
    • Singleton: se crea una sola vez y se reutiliza.

  4. PRINCIPIO DE INVERSIÓN DE DEPENDENCIAS (DIP)
  ─────────────────────────────────────────────────
  Establece que:
    a) Los módulos de alto nivel no deben depender de módulos de bajo nivel.
       Ambos deben depender de abstracciones.
    b) Las abstracciones no deben depender de detalles. Los detalles deben
       depender de las abstracciones.

  Esto se logra con interfaces y DI.

  5. ASP.NET CORE — VISIÓN GENERAL
  ─────────────────────────────────
  ASP.NET Core es el framework web de Microsoft para construir aplicaciones
  modernas. Su pipeline se basa en middleware:
    app.UseAuthentication();
    app.UseAuthorization();
    app.MapControllers();

  Cada middleware decide si procesa la solicitud o la pasa al siguiente.

  6. ACOPLAMIENTO VS COHESIÓN
  ────────────────────────────
  • Acoplamiento (coupling): grado de dependencia entre módulos. Buscamos
    bajo acoplamiento (loosely coupled).
  • Cohesión (cohesion): grado en que los elementos de un módulo pertenecen
    juntos. Buscamos alta cohesión.

  Una buena arquitectura maximiza la cohesión y minimiza el acoplamiento.

================================================================================
  [CÓDIGO — IMPLEMENTACIÓN PRÁCTICA]
================================================================================

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 1: Arquitectura en capas (estructura de proyecto)
  ──────────────────────────────────────────────────────────────────────────────

  Solución en Visual Studio:
    ─ MiApp.sln
      ├── MiApp.Domain          → Entidades, interfaces
      ├── MiApp.Application     → Servicios, DTOs
      ├── MiApp.Infrastructure  → EF Core, repositorios
      └── MiApp.WebAPI          → Controladores (presentación)

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 2: Capa de dominio (Domain)
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // MiApp.Domain/Entities/Producto.cs
  // ============================

  using System;

  namespace MiApp.Domain.Entities
  {
      public class Producto
      {
          public Guid Id { get; private set; }
          public string Nombre { get; private set; }
          public decimal Precio { get; private set; }
          public int Stock { get; private set; }
          public DateTime FechaCreacion { get; private set; }

          private Producto() { } // Para EF Core

          public Producto(string nombre, decimal precio, int stock)
          {
              Id = Guid.NewGuid();
              SetNombre(nombre);
              SetPrecio(precio);
              SetStock(stock);
              FechaCreacion = DateTime.UtcNow;
          }

          public void SetNombre(string nombre)
          {
              if (string.IsNullOrWhiteSpace(nombre))
                  throw new ArgumentException("El nombre no puede estar vacío.");
              Nombre = nombre;
          }

          public void SetPrecio(decimal precio)
          {
              if (precio <= 0)
                  throw new ArgumentException("El precio debe ser mayor a cero.");
              Precio = precio;
          }

          public void SetStock(int stock)
          {
              if (stock < 0)
                  throw new ArgumentException("El stock no puede ser negativo.");
              Stock = stock;
          }
      }
  }

  // ============================
  // MiApp.Domain/Interfaces/IProductoRepositorio.cs
  // ============================

  using System;
  using System.Collections.Generic;
  using System.Threading.Tasks;
  using MiApp.Domain.Entities;

  namespace MiApp.Domain.Interfaces
  {
      public interface IProductoRepositorio
      {
          Task<Producto> ObtenerPorIdAsync(Guid id);
          Task<IEnumerable<Producto>> ObtenerTodosAsync();
          Task<Producto> AgregarAsync(Producto producto);
          Task<Producto> ActualizarAsync(Producto producto);
          Task<bool> EliminarAsync(Guid id);
      }
  }

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 3: Capa de aplicación (Application)
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // MiApp.Application/DTOs/ProductoDTO.cs
  // ============================

  using System;

  namespace MiApp.Application.DTOs
  {
      public class ProductoDTO
      {
          public Guid Id { get; set; }
          public string Nombre { get; set; }
          public decimal Precio { get; set; }
          public int Stock { get; set; }
          public DateTime FechaCreacion { get; set; }
      }

      public class CrearProductoDTO
      {
          public string Nombre { get; set; }
          public decimal Precio { get; set; }
          public int Stock { get; set; }
      }

      public class ActualizarProductoDTO
      {
          public Guid Id { get; set; }
          public string Nombre { get; set; }
          public decimal Precio { get; set; }
          public int Stock { get; set; }
      }
  }

  // ============================
  // MiApp.Application/Servicios/ProductoService.cs
  // ============================

  using System;
  using System.Collections.Generic;
  using System.Linq;
  using System.Threading.Tasks;
  using MiApp.Application.DTOs;
  using MiApp.Domain.Entities;
  using MiApp.Domain.Interfaces;

  namespace MiApp.Application.Servicios
  {
      public interface IProductoService
      {
          Task<ProductoDTO> ObtenerPorIdAsync(Guid id);
          Task<IEnumerable<ProductoDTO>> ObtenerTodosAsync();
          Task<ProductoDTO> CrearAsync(CrearProductoDTO dto);
          Task<ProductoDTO> ActualizarAsync(ActualizarProductoDTO dto);
          Task<bool> EliminarAsync(Guid id);
      }

      public class ProductoService : IProductoService
      {
          private readonly IProductoRepositorio _repositorio;

          public ProductoService(IProductoRepositorio repositorio)
          {
              _repositorio = repositorio;
          }

          public async Task<ProductoDTO> ObtenerPorIdAsync(Guid id)
          {
              var producto = await _repositorio.ObtenerPorIdAsync(id);
              if (producto == null)
                  throw new KeyNotFoundException($"Producto {id} no encontrado.");
              return MapearADTO(producto);
          }

          public async Task<IEnumerable<ProductoDTO>> ObtenerTodosAsync()
          {
              var productos = await _repositorio.ObtenerTodosAsync();
              return productos.Select(MapearADTO);
          }

          public async Task<ProductoDTO> CrearAsync(CrearProductoDTO dto)
          {
              var producto = new Producto(dto.Nombre, dto.Precio, dto.Stock);
              var creado = await _repositorio.AgregarAsync(producto);
              return MapearADTO(creado);
          }

          public async Task<ProductoDTO> ActualizarAsync(ActualizarProductoDTO dto)
          {
              var producto = await _repositorio.ObtenerPorIdAsync(dto.Id);
              if (producto == null)
                  throw new KeyNotFoundException($"Producto {dto.Id} no encontrado.");

              producto.SetNombre(dto.Nombre);
              producto.SetPrecio(dto.Precio);
              producto.SetStock(dto.Stock);

              var actualizado = await _repositorio.ActualizarAsync(producto);
              return MapearADTO(actualizado);
          }

          public async Task<bool> EliminarAsync(Guid id)
          {
              return await _repositorio.EliminarAsync(id);
          }

          private static ProductoDTO MapearADTO(Producto p)
          {
              return new ProductoDTO
              {
                  Id = p.Id,
                  Nombre = p.Nombre,
                  Precio = p.Precio,
                  Stock = p.Stock,
                  FechaCreacion = p.FechaCreacion
              };
          }
      }
  }

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 4: Capa de infraestructura (Infrastructure)
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // MiApp.Infrastructure/Data/AppDbContext.cs
  // ============================

  using Microsoft.EntityFrameworkCore;
  using MiApp.Domain.Entities;

  namespace MiApp.Infrastructure.Data
  {
      public class AppDbContext : DbContext
      {
          public AppDbContext(DbContextOptions<AppDbContext> options)
              : base(options) { }

          public DbSet<Producto> Productos { get; set; }

          protected override void OnModelCreating(ModelBuilder modelBuilder)
          {
              modelBuilder.Entity<Producto>(entity =>
              {
                  entity.HasKey(p => p.Id);
                  entity.Property(p => p.Nombre)
                        .IsRequired()
                        .HasMaxLength(200);
                  entity.Property(p => p.Precio)
                        .HasColumnType("decimal(18,2)");
              });
          }
      }
  }

  // ============================
  // MiApp.Infrastructure/Repositories/ProductoRepositorio.cs
  // ============================

  using System;
  using System.Collections.Generic;
  using System.Threading.Tasks;
  using Microsoft.EntityFrameworkCore;
  using MiApp.Domain.Entities;
  using MiApp.Domain.Interfaces;
  using MiApp.Infrastructure.Data;

  namespace MiApp.Infrastructure.Repositories
  {
      public class ProductoRepositorio : IProductoRepositorio
      {
          private readonly AppDbContext _context;

          public ProductoRepositorio(AppDbContext context)
          {
              _context = context;
          }

          public async Task<Producto> ObtenerPorIdAsync(Guid id)
          {
              return await _context.Productos.FindAsync(id);
          }

          public async Task<IEnumerable<Producto>> ObtenerTodosAsync()
          {
              return await _context.Productos.ToListAsync();
          }

          public async Task<Producto> AgregarAsync(Producto producto)
          {
              _context.Productos.Add(producto);
              await _context.SaveChangesAsync();
              return producto;
          }

          public async Task<Producto> ActualizarAsync(Producto producto)
          {
              _context.Productos.Update(producto);
              await _context.SaveChangesAsync();
              return producto;
          }

          public async Task<bool> EliminarAsync(Guid id)
          {
              var producto = await _context.Productos.FindAsync(id);
              if (producto == null) return false;
              _context.Productos.Remove(producto);
              await _context.SaveChangesAsync();
              return true;
          }
      }
  }

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 5: Capa de presentación (Web API)
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // MiApp.WebAPI/Controllers/ProductosController.cs
  // ============================

  using System;
  using System.Collections.Generic;
  using System.Threading.Tasks;
  using Microsoft.AspNetCore.Mvc;
  using MiApp.Application.DTOs;
  using MiApp.Application.Servicios;

  namespace MiApp.WebAPI.Controllers
  {
      [ApiController]
      [Route("api/[controller]")]
      public class ProductosController : ControllerBase
      {
          private readonly IProductoService _productoService;

          public ProductosController(IProductoService productoService)
          {
              _productoService = productoService;
          }

          [HttpGet]
          public async Task<ActionResult<IEnumerable<ProductoDTO>>> ObtenerTodos()
          {
              var productos = await _productoService.ObtenerTodosAsync();
              return Ok(productos);
          }

          [HttpGet("{id:guid}")]
          public async Task<ActionResult<ProductoDTO>> ObtenerPorId(Guid id)
          {
              try
              {
                  var producto = await _productoService.ObtenerPorIdAsync(id);
                  return Ok(producto);
              }
              catch (KeyNotFoundException ex)
              {
                  return NotFound(ex.Message);
              }
          }

          [HttpPost]
          public async Task<ActionResult<ProductoDTO>> Crear([FromBody] CrearProductoDTO dto)
          {
              var producto = await _productoService.CrearAsync(dto);
              return CreatedAtAction(nameof(ObtenerPorId), new { id = producto.Id }, producto);
          }

          [HttpPut("{id:guid}")]
          public async Task<ActionResult<ProductoDTO>> Actualizar(Guid id, [FromBody] ActualizarProductoDTO dto)
          {
              if (id != dto.Id)
                  return BadRequest("El ID de la ruta no coincide con el cuerpo.");

              try
              {
                  var producto = await _productoService.ActualizarAsync(dto);
                  return Ok(producto);
              }
              catch (KeyNotFoundException ex)
              {
                  return NotFound(ex.Message);
              }
          }

          [HttpDelete("{id:guid}")]
          public async Task<ActionResult> Eliminar(Guid id)
          {
              var resultado = await _productoService.EliminarAsync(id);
              if (!resultado)
                  return NotFound();
              return NoContent();
          }
      }
  }

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 6: Configuración de DI en Program.cs
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // MiApp.WebAPI/Program.cs
  // ============================

  using Microsoft.EntityFrameworkCore;
  using MiApp.Application.Servicios;
  using MiApp.Domain.Interfaces;
  using MiApp.Infrastructure.Data;
  using MiApp.Infrastructure.Repositories;

  var builder = WebApplication.CreateBuilder(args);

  // Add services to DI container
  builder.Services.AddScoped<IProductoRepositorio, ProductoRepositorio>();
  builder.Services.AddScoped<IProductoService, ProductoService>();

  builder.Services.AddDbContext<AppDbContext>(options =>
      options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));

  builder.Services.AddControllers();
  builder.Services.AddEndpointsApiExplorer();
  builder.Services.AddSwaggerGen();

  var app = builder.Build();

  if (app.Environment.IsDevelopment())
  {
      app.UseSwagger();
      app.UseSwaggerUI();
  }

  app.UseHttpsRedirection();
  app.UseAuthorization();
  app.MapControllers();

  app.Run();

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 7: Inyección de dependencias — Concepto con interfaces
  ──────────────────────────────────────────────────────────────────────────────

  // Ejemplo simple de DI sin framework

  public interface ILogger
  {
      void Log(string mensaje);
  }

  public class ConsoleLogger : ILogger
  {
      public void Log(string mensaje)
      {
          Console.WriteLine($"[LOG] {mensaje}");
      }
  }

  public class FileLogger : ILogger
  {
      private readonly string _ruta;

      public FileLogger(string ruta)
      {
          _ruta = ruta;
      }

      public void Log(string mensaje)
      {
          File.AppendAllText(_ruta, $"[LOG] {mensaje}{Environment.NewLine}");
      }
  }

  public class Procesador
  {
      private readonly ILogger _logger;

      public Procesador(ILogger logger)  // DI: la dependencia se inyecta
      {
          _logger = logger;
      }

      public void Ejecutar()
      {
          _logger.Log("Procesador iniciado.");
          // ... lógica ...
          _logger.Log("Procesador finalizado.");
      }
  }

  // Uso:
  // ILogger logger = new ConsoleLogger();
  // var proc = new Procesador(logger);
  // proc.Ejecutar();

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 8: Middleware personalizado en ASP.NET Core
  ──────────────────────────────────────────────────────────────────────────────

  public class LoggingMiddleware
  {
      private readonly RequestDelegate _next;
      private readonly ILogger<LoggingMiddleware> _logger;

      public LoggingMiddleware(RequestDelegate next, ILogger<LoggingMiddleware> logger)
      {
          _next = next;
          _logger = logger;
      }

      public async Task InvokeAsync(HttpContext context)
      {
          _logger.LogInformation("Solicitud: {Method} {Path}",
              context.Request.Method, context.Request.Path);

          await _next(context);

          _logger.LogInformation("Respuesta: {StatusCode}",
              context.Response.StatusCode);
      }
  }

  // En Program.cs:
  // app.UseMiddleware<LoggingMiddleware>();

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 9: Opciones de configuración con IOptions<T>
  ──────────────────────────────────────────────────────────────────────────────

  // ============================
  // appsettings.json
  // ============================
  // {
  //   "ConfiguracionApp": {
  //     "NombreApp": "MiApp",
  //     "Version": "1.0.0",
  //     "MaximoIntentos": 3
  //   }
  // }

  public class ConfiguracionApp
  {
      public string NombreApp { get; set; }
      public string Version { get; set; }
      public int MaximoIntentos { get; set; }
  }

  // Registro en Program.cs:
  // builder.Services.Configure<ConfiguracionApp>(
  //     builder.Configuration.GetSection("ConfiguracionApp"));

  public class ServicioConfigurado
  {
      private readonly ConfiguracionApp _config;

      public ServicioConfigurado(IOptions<ConfiguracionApp> options)
      {
          _config = options.Value;
      }

      public void MostrarConfig()
      {
          Console.WriteLine($"App: {_config.NombreApp} v{_config.Version}");
      }
  }

  ──────────────────────────────────────────────────────────────────────────────
  Ejemplo 10: Implementación de Unit of Work
  ──────────────────────────────────────────────────────────────────────────────

  public interface IUnitOfWork : IDisposable
  {
      IProductoRepositorio Productos { get; }
      Task<int> CompleteAsync();
  }

  public class UnitOfWork : IUnitOfWork
  {
      private readonly AppDbContext _context;
      public IProductoRepositorio Productos { get; }

      public UnitOfWork(AppDbContext context, IProductoRepositorio productos)
      {
          _context = context;
          Productos = productos;
      }

      public async Task<int> CompleteAsync()
      {
          return await _context.SaveChangesAsync();
      }

      public void Dispose()
      {
          _context.Dispose();
      }
  }

================================================================================
  [RESUMEN DEL CAPÍTULO]
================================================================================

  En este capítulo has aprendido:
  • Qué es la arquitectura de software y por qué es importante.
  • La arquitectura en capas: presentación, aplicación, dominio e infraestructura.
  • La Inyección de Dependencias (DI) y cómo .NET Core la implementa.
  • El Principio de Inversión de Dependencias (DIP).
  • ASP.NET Core y su pipeline de middleware.
  • Cómo estructurar un proyecto real con capas bien definidas.
  • Patrones como repositorio y Unit of Work.

  Estos conceptos son la base del desarrollo profesional. Sin una arquitectura
  sólida, cualquier proyecto mediano o grande se vuelve inmantenible.

================================================================================
  [EJERCICIOS]
================================================================================

  1. Crea una solución con tres capas (Domain, Application, Infrastructure) y
     una entidad "Cliente" con propiedades: Id, Nombre, Email, Telefono.

  2. Implementa la interfaz IClienteRepositorio y su implementación en
     Infrastructure con una lista en memoria (no bases de datos aún).

  3. Crea un ClienteService en Application que use el repositorio mediante DI.

  4. Agrega un controlador ClientesController con endpoints GET, POST, PUT, DELETE.

  5. Registra todos los servicios en Program.cs usando Scoped.

  6. Añade un middleware personalizado que mida el tiempo de cada solicitud.

  7. Agrega una interfaz IEmailService y una implementación SmtpEmailService,
     luego inyéctala en ClienteService para enviar un correo al crear un cliente.

  8. Implementa la validación con Data Annotations en los DTOs.

  9. Crea un DTO de respuesta estándar (ApiResponse<T>) con propiedades:
     Success, Message, Data, Errors.

  10. Refactoriza el controlador para usar el ApiResponse<T> en todas las respuestas.

================================================================================
  [PREGUNTAS FRECUENTES — FAQ]
================================================================================

  P: ¿Debo usar siempre tres capas?
  R: No siempre. Para proyectos pequeños una arquitectura de una sola capa
     puede bastar. La regla es: la complejidad de la arquitectura debe
     igualar la complejidad del dominio.

  P: ¿Cuál es la diferencia entre Scoped y Transient?
  R: Transient crea una nueva instancia cada vez que se pide. Scoped crea una
     por solicitud HTTP (o por scope). Singleton crea una única instancia
     para toda la vida de la aplicación.

  P: ¿EF Core debe estar en Infrastructure o Data?
  R: En Infrastructure. La capa de dominio no debe tener dependencias de
     infraestructura como bases de datos.

  P: ¿Qué es el "problema del castillo de naipes"?
  R: Ocurre cuando cambios en una capa baja (ej. base de datos) afectan a
     todas las capas superiores. Una buena arquitectura con interfaces evita esto.

  P: ¿Puedo mezclar arquitectura en capas con Clean Architecture?
  R: Sí. Clean Architecture es una evolución de capas. La idea central es la
     misma: dependencias hacia adentro.

================================================================================
  [DESAFÍO DEL ARQUITECTO]
================================================================================

  Diseña e implementa una mini-arquitectura para un sistema de "Gestión de
  Biblioteca" que incluya:

  • Entidades: Libro, Autor, Prestamo, Usuario.
  • Capa Domain: entidades e interfaces.
  • Capa Application: servicios y DTOs con validación.
  • Capa Infrastructure: repositorios en memoria.
  • Capa Presentation: Web API con endpoints completos.

  Todos los servicios deben inyectarse mediante DI. El middleware debe
  registrar cada operación en un archivo de log. Incluye un manejador global
  de excepciones.

  EXTRA: Implementa un patrón Specification para filtros avanzados.

================================================================================
  [CIERRE DEL CAPÍTULO]
================================================================================

  Has dado el primer paso hacia la maestría en arquitectura de software.
  Recuerda: un buen arquitecto no solo escribe código que funciona, sino
  código que perdura. La separación de responsabilidades, la inyección de
  dependencias y el diseño por capas son herramientas que te acompañarán
  toda tu carrera.

  En el próximo capítulo abordaremos Entity Framework Core, el ORM más
  potente del ecosistema .NET. Prepárate para conectar tu arquitectura
  con una base de datos real.

  ¡Nos vemos en el Capítulo 72!

  ──────────────────────────────────────────────────────────────────────────────
  © 2026 C# MASTER BOOK — Profesor Búho AI
  "El conocimiento no pesa, ilumina."
  ──────────────────────────────────────────────────────────────────────────────
  [FIN DEL CAPÍTULO 71]
